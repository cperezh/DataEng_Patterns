services:
  etl:
    build: .
    restart: "no"
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: test
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: 1234
    command: ["python", "src/main.py"]
    volumes:
      - ./data:/app/data